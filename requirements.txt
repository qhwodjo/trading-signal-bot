# ─── Core ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
aiohttp>=3.9.0
asyncio

# ─── Data Sources ─────────────────────────────────────────────────────────────
yfinance>=0.2.36
ccxt>=4.2.0
requests>=2.31.0
feedparser>=6.0.10

# ─── Technical Analysis ────────────────────────────────────────────────────────
pandas-ta>=0.3.14b
ta>=0.10.2
numpy>=1.24.0
pandas>=2.0.0

# ─── NLP / Sentiment ──────────────────────────────────────────────────────────
transformers>=4.35.0
torch>=2.0.0          # CPU-only: install via pip install torch --index-url https://download.pytorch.org/whl/cpu
nltk>=3.8.1

# ─── Backtesting ──────────────────────────────────────────────────────────────
vectorbt>=0.26.1      # optional, falls back to manual simulation if unavailable
# backtesting>=0.3.3  # alternative

# ─── Visualization ────────────────────────────────────────────────────────────
mplfinance>=0.12.10b0

# ─── Misc ─────────────────────────────────────────────────────────────────────
scipy>=1.11.0
beautifulsoup4>=4.12.0

# ─── Notes ────────────────────────────────────────────────────────────────────
# For LOW-MEMORY VPS (<512MB): skip torch + transformers and use NLTK VADER only.
# Comment out the transformers and torch lines and uncomment below:
# nltk>=3.8.1  (already included)
# The bot will automatically fall back to VADER if FinBERT is unavailable.
#
# Install order (avoids conflicts):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
